{
  "name": "Decentralized-ETF-Arbitrage",
  "version": "1.0.0",
  "description": "A proof-of-concept for a Decentralized ETF Arbitrage Network.",
  "scripts": {
    "compile": "npx hardhat compile",
    "test": "npx hardhat test",
    "deploy": "npx hardhat run scripts/deploy.js --network localhost"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^2.0.0",
    "hardhat": "^2.12.0"
  }
}